# MCPGate standalone legacy

Esta carpeta preserva la implementación standalone previa de MCPGate después de la migración a monorepo.

El contenido queda pendiente de triage y migración conservadora hacia `apps/api` o futuros paquetes compartidos si aparece una necesidad real.
